# sgm-accelerator
An FPGA-accelerated implementation of the Semi-Global Matching (SGM) algorithm using High-Level Synthesis (HLS). This project focuses on optimizing the matching cost aggregation core for real-time stereo vision applications.
